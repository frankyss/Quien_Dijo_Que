var sec = 00;
    var min = 2;
    timeCount.innerHTML = (min < 10 ? "0" + min : min) + ":" + (sec < 10 ? "0" + sec : sec);
    var sec = 00;
    var min = 2;
    timeCount.innerHTML = (min < 10 ? "0" + min : min) + ":" + (sec < 10 ? "0" + sec : sec);
    